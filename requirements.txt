# Western Astrology Calculator - Runtime Dependencies
# Install with: pip install -r requirements.txt

geopy>=2.4
timezonefinder>=6.0
kerykeion>=5.0
sv-ttk>=2.5   # Provides modern Windows 11 (Sun Valley) native-looking ttk theme
reportlab>=4.0  # For generating nice PDF natal chart reports
cairosvg>=2.7   # For converting the SVG chart wheel to PNG for embedding in PDF (optional but recommended for image in PDF)

# Optional but recommended for building standalone executables:
# pyinstaller>=6.0
